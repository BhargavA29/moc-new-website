/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com', 'img.icons8.com', 'www.pngplay.com', 'upload.wikimedia.org'],
  },
}

module.exports = nextConfig